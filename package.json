{
  "name": "insightai",
  "private": true,
  "description": "Scripts de conveniencia. El código vive en backend/ y frontend/.",
  "scripts": {
    "dev:backend": "cd backend && .venv\\Scripts\\python.exe -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:frontend": "npm run dev --prefix frontend -- --port 3000",
    "test:backend": "cd backend && .venv\\Scripts\\python.exe -m pytest tests -v"
  }
}
