# Environment variables for Docker Compose (docker-compose.yml)
# Copy this file to .env and set real values before running `docker compose up -d`.

# PostgreSQL password for the insight_ai user (required — compose fails without it).
# The backend must use the same value in backend/.env (POSTGRES_PASSWORD).
POSTGRES_PASSWORD=
