# Production: public URL of the FastAPI service (no trailing slash).
# Leave unset in local dev — Next.js rewrites /api/* to http://127.0.0.1:8000.
NEXT_PUBLIC_API_URL=https://insightai-api.onrender.com
